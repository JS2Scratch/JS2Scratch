//!strict

var partOne = "hello "
var partTwo = "world";
var res = "hello world";

if (operation.join(partOne, partTwo) === res) {
    // "console.log" uses "say" for debug purposes
    console.log(operation.join(partOne, partTwo, " is the same as ", res));
}

